# Internal Document Archive

This directory preserves historical internal docs that are no longer default
agent context. They may be useful for archaeology, but they are not current
workflow, runtime, product, or harness contracts.

Active internal docs live under `docs/`. Published user docs live under `doc/`.

I’ll use the `obsidian-runtime-debug` skill because this is a lifecycle/runtime Obsidian bug, and I’ll start by reading the repo-owned stage contract and source issue before making any claims.The source issue is present and contains the scoped bug claim, so this is not context-blocked. I found a prior memory note that issue #33 is a split bug cluster; I’ll use it only to keep scope narrow and will rely on current repo/runtime evidence for the investigation.The stage spec confirms this is a middle-flow investigation: runtime evidence and a bounded RCA artifact only, with no product/test/source edits. I’m going to read the evidence/CDP docs and the existing `rca-review.md` next, because this stage is a continuation when that review exists.There is no populated `docs/agent/runs/BLP-2/rca-review.md`, but the source snapshot explicitly names the parent RCA review as the canonical prior artifact. I’ll read that named file plus the CDP inventory/gates, then use the fixed-port runtime path from the stage spec.The named parent RCA file is not present in this worktree, so I’ll record that as an evidence limitation instead of inferring from it. The OpenSpec gate keeps this as direct bug investigation: restoring existing inline-edit behavior, no proposal needed unless a later design would change intended behavior.Dependency install returned success after refreshing `node_modules`; I’ll verify the CDP client dependency and rebuild the plugin bundle before starting/reusing the fixed Obsidian runtime on port `19225`.The CDP dependency resolves from this worktree and the only git-visible state is the run directory. I’m building now, then I’ll check the fixed CDP target before launching anything new.`build-with-types` passed and regenerated the plugin bundle. I’ll check whether that touched tracked files, then use the fixed CDP port workflow.No existing BLP runtime is listening on `19225`, so I’m launching the repo’s isolated Obsidian debug environment on that exact port as required.The npm wrapper passed a literal `--` through to PowerShell and failed before starting Obsidian. I’ll use the underlying repo launcher directly with `-Port 19225`, which is the same fixed-port runtime path without the argument forwarding issue.The isolated runtime is ready on `19225` with Block Link Plus loaded from this worktree. I’ll now gather static owner mapping from the inline-edit lifecycle code and create a repo-local temporary CDP probe to reproduce the A -> B -> A file-switch failure.The static owner mapping matches the task’s hypothesis: `layout-change` schedules `refreshLivePreviewObservers()` without a forced rescan, while the observer method short-circuits existing views unless forced. I’m reading the cleanup and mount code before writing the runtime probe so the probe can measure the right DOM states.I have the line references for the suspected lifecycle path: observer refresh, hidden-embed cleanup, shell cleanup, and detached leaf cleanup. The probe will preserve plugin settings, use temporary notes, and test both the broken return-to-A state and whether a forced rescan remounts the existing DOM.The temporary CDP probe is in `.tmp/` and only touches the disposable vault/settings during execution, with restoration in `finally`. I’m running it against the fixed runtime now.The probe reproduced the exact scoped symptom and showed that `refreshLivePreviewObservers(true)` remounts the existing A embed DOM. I’m adding one extra control to the probe: explicitly call the non-forced refresh after returning to A, so the artifact can distinguish “ordinary refresh does not rescan” from “forced refresh remounts.”I’m rerunning the updated probe now against the same isolated runtime.The updated probe confirms the control case: the non-forced refresh leaves A broken, and the forced rescan restores `.blp-inline-edit-root` and `.blp-inline-edit-host`. I’ll do a final runtime/settings sanity check, then write the investigation handoff.A preexisting `docs/agent/runs/BLP-2/investigation.md` is already in the untracked run directory, so I’ll read it and update it rather than blindly replacing it.The existing artifact is an older Runtime Blocked handoff. Since the runtime is now verified and the repro passed, I’ll replace it with the current successful investigation evidence while preserving the fact that the earlier wrapper launch mode failed.I’m updating the canonical investigation artifact now. It will mark the issue confirmed, include the runtime state table, and keep the fix guidance bounded to the remount/rescan lifecycle path.The handoff artifact is updated. I’m doing a final read/status pass to catch formatting issues and record the current git state.Completed the BLP-2 runtime investigation and updated [investigation.md](C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-2/docs/agent/runs/BLP-2/investigation.md).

Result: confirmed bug. The A -> B -> A probe reproduced the loaded embed shell losing `.blp-inline-edit-root` and `.blp-inline-edit-host`; explicit non-forced refresh did not remount it, while `refreshLivePreviewObservers(true)` did. RCA is the inline-edit Live Preview observer/remount lifecycle: hidden-leaf cleanup detaches the managed embed, then the unchanged observed view is not rescanned on return.

Validation run: dependency install, `ws` resolution, `corepack pnpm run build-with-types`, fixed-port Obsidian/CDP launch on `19225`, and the temporary CDP repro in `.tmp/`. No product source/tests/specs were edited; only the investigation artifact was updated. Current git status remains `?? docs/agent/runs/BLP-2/`.


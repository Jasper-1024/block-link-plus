I’ll first load the repo contract and the relevant runtime-debug skill, then verify whether the issue context is complete before touching code. Since this is an Obsidian lifecycle/editor bug, I’ll treat runtime evidence as required unless the stage docs say otherwise.Memory has prior BLP runtime-debug notes, mostly as workflow guardrails; I’ll still verify the current repo state instead of relying on them. Next I’m reading the stage contract and issue snapshot so the implementation artifact matches the runner’s expected format.The issue snapshot is complete and scoped narrowly to the file-switch remount failure, so this is not context-blocked. I’m now reading the accepted investigation/design artifacts and validation rules; if the design review is not accepted, I’ll stop before editing.The design review is accepted, but the worktree already contains source/test/CDP changes before my edits. I’m treating those as pre-existing work and inspecting them carefully before deciding whether anything still needs to be changed.The existing patch largely matches the accepted design and already has an implementation artifact, but I’m not going to rely on stale validation text. I’ll rerun the targeted tests, full suite, build, and the fixed-port CDP checks, then update the artifact only if the current run’s evidence differs.Dependencies are present and the diff has no whitespace errors; Git only reports its usual LF-to-CRLF warning for the TypeScript file. I’m running the focused observer regression first so any failure points at the lifecycle change directly.The focused test passes: it covers unchanged visible, hidden recording, hidden-to-shown requeue, and forced rescan. I’m moving to the full Jest suite now to catch any regressions in the broader inline-edit surface.Full Jest passes with the same open-handle warning noted in the artifact, so the source-level regression looks stable. I’m building with types next; after that I’ll refresh the Obsidian runtime and run the CDP regressions against the built plugin.Typed build passed and regenerated the plugin bundle. I’m checking the fixed CDP port now; if the existing target is the BLP debug runtime, I’ll reuse it and reload before trusting runtime evidence.The fixed port has the expected BLP Obsidian target. Since the build just changed `main.js`, I’m reloading the plugin in that runtime before running the new leaf-switch regression and adjacent inline-edit checks.The plugin reload in the debug runtime succeeded (`2.0.15`). I’m running the new A -> B -> A CDP regression now; this is the gate that proves the fix works without manual forced rescan.The leaf-switch regression passes and shows the returned host has exactly one root and host again. I’m running the adjacent jump-affordance and bottom-padding snippets sequentially because they share the same Obsidian runtime state.The jump-affordance snippet passes with one native link and one inline-edit host/root. I’m running the bottom-padding check now, then I’ll update the implementation artifact with the current command evidence and final git status.The CDP runtime checks pass. Before I edit the stage artifact, I’m going to make its validation section reflect this current run rather than stale command output, including that dependencies were already present and the plugin was reloaded after the build.The artifact is updated with the current validation run. I noticed one remaining sentence in the artifact describes an earlier event probe, so I’m tightening that wording to avoid overstating what I re-ran in this pass.There is already a same-run `code-review.md` in the artifact directory, although this stage did not require it as an input. I’m going to read it once because the runner listed the path and it may contain a prior review finding against the current patch.The existing same-run code review is a `needs-revision` artifact: it found the jump-affordance CDP check could snapshot too early and fail on transient system tail tokens. My sequential rerun passed, but to close that review finding properly I’m inspecting the snippet and will make the wait condition stable instead of relying on timing luck.The jump-affordance snippet was waiting for the root, sleeping 300 ms, then asserting no tail tokens. I’m changing that to poll the full stable state it actually requires: one active inline embed, one direct native jump link, one host/root, visible link, and no system tail tokens.The snippet syntax checks out. I’m making the readiness predicate a named boolean so the CDP regression is easier for the next reviewer to audit.The validation snippet now waits for the final stable condition instead of a fixed delay. I’ll rerun the required unit/full/build and all three CDP checks after this revision so the implementation artifact can supersede the prior `needs-revision` review cleanly.The adjusted CDP snippet passes `node --check`, and the focused observer test still passes. I’m rerunning the full suite and typed build next.Full Jest still passes with the known open-handle warning. I’m building again so the runtime checks use the current compiled bundle after the snippet revision.The build passed again. I’m reloading the plugin in the fixed-port runtime one more time, then rerunning the leaf-switch, jump-affordance, and bottom-padding CDP snippets sequentially.The leaf-switch regression still passes after the rebuild and plugin reload. I’m rerunning the revised jump-affordance snippet now, which is the check that previously blocked code review.The revised jump-affordance check now passes with one direct native link and one host/root, and no tail-token leak in the stable snapshot. I’m running the bottom-padding snippet once more as the last runtime gate.All required validation now passes after the code-review-driven snippet revision. I’m updating `implementation.md` so it records the new CDP snippet change, the re-run commands, and that the prior jump-affordance validation blocker has been addressed in this implementation pass.The final diff is narrow: the lifecycle source file, the new unit and CDP regression, the stabilized adjacent CDP snippet, and the implementation handoff. I’m adding the final `git status --short` to the artifact because the repo workflow asks each run to record it.The final whitespace check passes; Git only repeats the LF-to-CRLF warning for the edited files. I’m marking the implementation run complete with `ready-for-review` and no remaining validation blocker.I’m pulling the exact memory line numbers for the final citation block because I used prior BLP runtime-debug memory only as workflow context at the start.Implemented and revalidated BLP-2. The run is recorded as `ready-for-review` in [implementation.md](</C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-2/docs/agent/runs/BLP-2/implementation.md>).

Changed:
- [InlineEditEngine.ts](</C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-2/src/features/inline-edit-engine/InlineEditEngine.ts:65>): tracks Live Preview view visibility and requeues existing embed DOM on hidden -> shown return.
- Added [InlineEditEngine.live-preview-observer.test.ts](</C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-2/src/features/inline-edit-engine/__tests__/InlineEditEngine.live-preview-observer.test.ts:1>).
- Added [inline-edit-embed-leaf-switch-remount.js](</C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-2/scripts/cdp-snippets/inline-edit-embed-leaf-switch-remount.js:1>).
- Stabilized [inline-edit-embed-jump-affordance.js](</C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-2/scripts/cdp-snippets/inline-edit-embed-jump-affordance.js:84>) to address the prior code-review validation flake.

Validation passed:
- `node --check scripts/cdp-snippets/inline-edit-embed-jump-affordance.js`
- `corepack pnpm test -- InlineEditEngine.live-preview-observer`
- `corepack pnpm test` passed, with the existing Jest open-handle warning.
- `corepack pnpm run build-with-types`
- Fixed-port CDP plugin reload on `19225`
- CDP leaf-switch remount regression
- CDP jump-affordance regression
- CDP bottom-padding regression

Final `git status --short` is recorded in the implementation artifact. I did not rewrite the existing `code-review.md`; it remains the prior review artifact, while `implementation.md` now records that its blocker was addressed and revalidated.


I’ll use the repo’s Obsidian runtime-debug workflow for this RCA review because the issue is about editor lifecycle behavior. I’m starting with the required stage and context files, then I’ll verify whether the existing RCA is supported by repo and runtime evidence.The first memory lookup found only general BLP runtime-debug context; I’m not going to use historical run archives. The skill path in the session list is repo-local, so I’m correcting that and reading the RCA-review contract plus the current run’s canonical artifacts.The prior investigation artifact is present and detailed; the later-stage artifacts are not present, which is expected for an RCA-review gate. I’m now checking the validation contracts and the specific source paths the investigation used so the review can distinguish observed facts from inference.The static path matches the investigation’s hypothesis: hidden-leaf cleanup can remove the inline-edit host/root, and the normal layout observer refresh skips unchanged roots. I’m checking the fixed CDP runtime now and will rerun the existing local probe if the runtime is still available.The fixed `19225` target is still the isolated BLP runtime, so I can validate without launching another app instance. I’m rerunning only the scoped A -> B -> return probe, which stays inside `.tmp` and the disposable vault.The rerun reproduced the exact scoped failure and confirmed the forced-rescan contrast: non-forced refresh leaves host A native-only, forced rescan restores the inline-edit root and host. I’m reading the relevant function bodies now so the artifact can name only the mechanism proven by source and runtime.I’m writing the RCA review artifact now. It will accept the owner layer and runtime mechanism, but it will keep the later implementation trigger as a design-stage decision instead of smuggling a fix plan into this review.The review artifact is in place. I’m doing a final artifact read plus whitespace/status checks so the handoff is clean and scoped to the required file.The artifact passes `git diff --check`. I’m grabbing the exact memory citation lines now because I used the repo-runtime memory note at the start, then I’ll close with the result and validation.Completed the RCA review artifact at [docs/agent/runs/BLP-2/rca-review.md](C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-2/docs/agent/runs/BLP-2/rca-review.md).

Verdict: `accepted`. I independently reran the fixed-port CDP check and the scoped `.tmp/blp2-inline-edit-leaf-switch-repro.js` probe. The repro again showed A returns without inline-edit root/host, non-forced refresh does not restore it, and forced rescan does.

Validation:
- `node scripts/obsidian-cdp.js list` on `19225`: found isolated BLP Obsidian target.
- `node scripts/obsidian-cdp.js eval-file ".tmp/blp2-inline-edit-leaf-switch-repro.js"`: `ok: true`, `forcedRescanRestored: true`.
- `git diff --check -- docs/agent/runs/BLP-2/rca-review.md`: clean.

No product source, tests, package metadata, generated files, CDP snippets, or OpenSpec specs were edited. Current git status still shows the run directory as untracked: `?? docs/agent/runs/BLP-2/`.


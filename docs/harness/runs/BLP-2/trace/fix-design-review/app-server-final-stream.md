I’ll use the Obsidian runtime-debug skill for this repo’s runtime-facing bug review, and I’ll do the required repo/stage reads before judging the fix design.The issue snapshot is present and contains the scoped bug claim, so this is not context-blocked. The memory pass points to prior BLP inline-edit/CDP work, but I’ll verify the current artifacts and repo state rather than carrying those facts forward blindly.I’ve got the stage contract now: this is review-only, so I won’t touch product code, tests, snippets, package files, generated output, or specs. Next I’m reading the canonical handoff inputs and then I’ll verify the design’s code assumptions against the current source.The fix design is in the expected `ready-for-review` state, so I can perform a real design review rather than context-blocking. I’m checking the exact source paths it relies on now, especially the observer early-return, hidden cleanup, and idempotent remount guards.The main design axis is sound against the current code: `ensureLivePreviewObserver()` really does skip unchanged roots, and the current mount path has already-mounted and pending guards. The sharp edge I’m checking now is whether the proposed `lastShown` transition can miss cases where Obsidian does not enumerate the hidden leaf before it becomes visible again.There’s no repo-local `.tmp` repro file in this worktree despite the earlier artifacts mentioning it, so I’m not relying on it as an input. The fixed-port Obsidian target is still reachable, so I’m using a read-only CDP snapshot to test the visibility/enumeration assumption the design depends on.The review is trending accepted, with implementation guardrails rather than redesign. I found source support for the design’s assumptions, and the live Obsidian snapshot shows hidden markdown tab leaves are still returned by `getLeavesOfType("markdown")`, which is the key condition for the proposed hidden-to-shown memory to work.I’ve written the review artifact with an accepted verdict and the constraints the implementation worker needs. I’m doing the final sanity check now: artifact content and end-state git status.Created [fix-design-review.md](<C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-2/docs/agent/runs/BLP-2/fix-design-review.md>) with verdict: `accepted`.

Key outcome: the design is ready for implementation with the narrow `InlineEditEngine` hidden-to-shown visible-return rescan. I verified the source assumptions, checked OpenSpec gate state, and ran a read-only CDP snapshot on port `19225` confirming hidden markdown tab leaves are still enumerated with `containerShown=false`.

No product code, tests, package files, generated files, CDP snippets, or specs were changed. Final `git status --short` remains `?? docs/agent/runs/BLP-2/`.


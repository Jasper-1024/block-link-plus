I?l first load the repo-owned stage contract and the issue snapshot, then I?l only branch into runtime/spec work if those files require it. I? also using the Obsidian runtime debug skill because this bug is about real editor behavior.The issue snapshot is present and contains the full scoped bug claim, so this is not context-blocked. The contract says implementation must be driven by accepted fix design and review, so I? loading those handoff artifacts before touching code.The design review is accepted, so implementation can proceed. The patch boundary is narrow: `selectiveEditor.ts` plus a CM6 history regression, followed by Jest/build and fixed-port CDP evidence.The worktree already has untracked runner artifacts, so I?l leave those alone and only edit the scoped implementation/test files plus the required implementation handoff. I? reading the existing selective-editor tests now to keep the new regression in the local style.The current code matches the RCA: range updates are still annotation specs returned from a transaction filter. I? going to extract that line-range math into an effect-producing helper and wire it through a transaction extender, leaving the filter as the out-of-range edit blocker.I? applying the source change now. The important behavioral choice is that the new extender uses effects, not annotations, because the existing state fields already consume range effects and CM6 history transactions still allow extenders under `filter: false`.The source patch is in place. I? adding the regression in the existing `selectiveEditor.systemLine.test.ts` file so it exercises the same DOM hiding behavior and directly checks the range state fields through undo and redo.The regression now covers the exact child issue path and redo? shared history mechanism. I? running the targeted Jest file first, then I?l broaden to the required full validation.The targeted test passes, including the new undo/redo assertion. I? moving through the required repo validation now: frozen install, full Jest, then type/build.The first frozen install hit pnpm? ?einstall node_modules???prompt path, so I? rerunning it with `CI=1` to make the install check non-interactive and unambiguous.The non-interactive frozen install completed and recreated `node_modules` from the lockfile. I? running the full Jest suite next; after that I?l run `build-with-types`.Full Jest passed with the repo? existing worker-teardown warning at the end. I? running the production type/build command now, then I?l reload or launch the fixed-port Obsidian runtime for CDP validation.Type/build passed. I? checking the required fixed CDP port `19225` now; if the current target is the BLP debug vault I?l reuse it and reload the plugin before the repro.Port `19225` is a reusable BLP Obsidian target. I? confirming the vault/plugin state, then I?l run the inline-edit runtime repro against the rebuilt plugin.The debug vault is `blp` and Block Link Plus is loaded. Since the bundle was rebuilt, I? reloading the plugin inside that runtime before taking CDP evidence.The plugin reload succeeded. I need a one-off runtime probe for the exact issue flow, so I?l keep it under `.tmp/` and leave reusable CDP snippets untouched.The repo snippets show the existing inline-edit debug surfaces expose the CM ranges on the embedded editor, so the temporary repro can assert state directly rather than relying only on DOM text. I? creating that probe under `.tmp/`.I noticed the first version of the temporary probe had a brittle `window.require` check around undo. I? tightening it so the repro focuses the embedded CM editor and uses Obsidian? undo command path, with a keyboard-event fallback only if that command ID is unavailable.The temporary repro is ready. I? running it through the fixed-port CDP client now and will inspect any failure rather than paper over it.The CDP probe failed while serializing a thrown error because I attached live DOM/CM objects to the exception details. I? adjusting the temporary probe to report compact snapshots only, then I?l rerun it.The exposed `__blpInlineEdit...Range` properties are initial debug snapshots, not live state updates, so that first probe was checking stale debug fields after the edit. I? doing a small state-inspection pass to find a reliable live range readout for the CDP assertion.The state inspection still returned object-heavy CM internals. I? narrowing it to primitive two-number arrays only so CDP can serialize it.I trimmed the state inspection to primitive array candidates only. I? rerunning it now.The live CM state values show the range fields do update to `[5,10]`; the older debug properties are stale. I? updating the main repro to assert against those live primitive range fields.The repro now reads live state-field candidates instead of stale debug snapshots. I? rerunning the exact Enter/undo flow.Using `app.commands.executeCommandById("editor:undo")` reproduced the stale range, which is useful but not the exact keyboard path from the issue. I? switching the CDP validation to real key events: setup and focus the embedded CM, send `Enter`, snapshot, send `Ctrl+Z`, then snapshot again.The keyboard setup script is in place. I? adding a small snapshot/cleanup script so each phase can assert the live ranges and DOM leak status after CDP key events.I? adding a cleanup helper for the temporary vault notes so the runtime state is restored after the probe.The keyboard CDP scripts are ready. I?l run setup, send a real `Enter`, snapshot live ranges, send real `Ctrl+Z`, snapshot again, then clean up the temporary notes.Setup succeeded: the embedded editor is focused, live ranges are `[5,9]`, and the following paragraph is hidden. I? sending the real Enter key now.After real Enter, the embedded doc has 12 lines, both live ranges are `[5,10]`, and the paragraph is still hidden. I? sending the real Ctrl+Z now.The exact keyboard runtime still fails: after real Ctrl+Z the doc shrinks to 11 lines but both live ranges remain `[5,10]`, and the paragraph leaks. That contradicts the accepted ?xtender-only is sufficient??design in the real Obsidian path, so I? checking whether this is a design mismatch or a small missing piece before deciding the gate verdict.The runtime failure is now clear enough to inspect the transaction itself. I? cleaning up that repro run, then I?l do one instrumented keyboard run to see why the extender condition does not shrink the range on the actual Obsidian undo transaction.I? setting up a fresh keyboard repro and adding a dispatch logger around the embedded editor. The logger records the undo transaction? line delta, touch range checks, live range values, and any primitive range effects on the transaction.The dispatch logger is written. I?l run a clean setup, send Enter, attach the logger at the `[5,10]` state, send Ctrl+Z, and fetch the recorded transaction summary.The instrumented undo transaction is the real mismatch: it is `userEvent: undo`, doc lines 12 to 11, touches inside the range, but carries no primitive range effects and the live ranges remain `[5,10]`. I? checking the Enter transaction too, to separate ?xtender never runs under keyboard??from ?xtender runs for normal input but not for history undo.?he missing piece is CM history effect inversion: the extender updates the range on normal input, but Obsidian? actual undo transaction does not get extender effects. I? adding the bounded history integration in the same selective-editor layer so undo/redo carry the previous range values through `invertedEffects`.The source now includes the history inversion facet alongside the extender. I? rerunning the targeted Jest file first to catch any local CM6 regressions before rebuilding for Obsidian.The targeted test still passes. I? running the full test/build cycle again because the source patch changed after the prior full validation.Full Jest and build passed again, with the same Jest worker-teardown warning. I? cleaning up the prior runtime probe state, reloading the rebuilt plugin, then rerunning the fixed-port keyboard repro.The rebuilt plugin is reloaded. I? running the real keyboard sequence now: setup, `Enter`, `Ctrl+Z`, and `Ctrl+Y` redo, with live range snapshots after each key.The `invertedEffects` path also did not affect Obsidian? actual history command, which points to a runtime-package mismatch around CM history. I? replacing that with a same-layer view-update fallback: when a doc-changing transaction leaves range fields stale, it dispatches a non-history range-effect repair immediately after the update.The fallback repair is now in source. I? rerunning targeted tests and then the full validation/build again before testing the real keyboard path.The targeted history regression still passes. I? running the full suite and type/build after the repair-hook change.Full tests and build passed again with the same worker-teardown warning. I? reloading the rebuilt plugin and rerunning the fixed-port keyboard repro.The plugin reload succeeded. I? running setup, real Enter, real Ctrl+Z, and redo again.The runtime still shows stale ranges after undo, so the rebuilt extension is not taking effect in the existing editor state the way a plugin reload suggests. I? checking whether the active CM already has an older appended config and whether the new repair hook is present in a freshly launched debug runtime.A plugin enable/disable reload is not enough evidence here because existing CM states can retain appended extensions. I? doing a full Obsidian window reload on the fixed-port debug target, then I?l re-check plugin load and rerun the keyboard repro.The full window reload completed and the plugin is loaded in the BLP debug vault. I? cleaning the old temp notes and rerunning the keyboard repro in this refreshed state.The real fix needs to live in the range state fields themselves. That path runs for every transaction, including Obsidian? actual undo transaction, and avoids relying on CM extension hooks that the runtime history path is bypassing. I? moving the line-range sync into the field updates and removing the duplicate extender/listener maintenance.The state fields now compute line-count range changes directly. I? removing the now-redundant extender/listener maintenance so there is only one range-sync owner.There is now a single range-maintenance owner: the two state fields. I? rerunning the focused test before rebuilding.The focused test passes with field-owned range sync. I? running the full suite and type/build one more time.Full tests and build pass with the usual Jest teardown warning. I? doing a full Obsidian window reload again so the rebuilt field-level code is definitely in the embedded editor state.The window reload is complete and the plugin is loaded. I? running the fixed-port keyboard validation again.The field hook is present, but the live undo path is still preserving a stale explicit range value before the automatic sync can run. I? changing the field update order so line-count synchronization overrides stale range carriers on doc-changing transactions; initial mount is unaffected because it does not change doc lines.The override order is patched. I? rerunning the focused test, then build/runtime if it stays green.Focused regression still passes. I? running full tests and build one more time before reloading Obsidian.Full validation/build is still green. I? reloading the debug window and rerunning the keyboard repro.The repeated runtime failure may be stale plugin code in the existing Electron process rather than the rebuilt bundle, since Obsidian can keep plugin modules alive across renderer reloads. I? closing the fixed-port debug window and starting a fresh isolated runtime on `19225` so validation uses a cold load of `main.js`.The fixed-port runtime is closed; the connection is refused, so the port is free. I? starting a fresh isolated Obsidian runtime on `19225`.The pnpm wrapper passed a literal `--` through to PowerShell on this Windows shell, so the launcher rejected it. I? invoking the repo script directly with `-Port 19225`, which is the same launcher without the wrapper argument bug.A fresh isolated runtime is ready on `19225`, with the plugin loaded from this worktree junction. I? rerunning the keyboard validation in that cold process.The cold-runtime keyboard repro now passes: undo returns live ranges to `[5,9]` and the paragraph stays hidden; redo returns `[5,10]` without leaking. I? running the two required adjacent CDP snippets next.The two CDP snippets interfered because one toggles the plugin while the other was running. I? resetting the plugin state and rerunning them sequentially.The plugin is loaded again. I? running `debug-inline-edit-system-line.js` first, then the jump-affordance snippet after it finishes.The system-line snippet passed. I? running the adjacent inline-edit jump-affordance regression now.The jump-affordance snippet is failing on system-tail text leakage, so I? checking whether that is a new regression from this patch or a pre-existing snippet/settings interaction. I?l inspect the live active embed and settings before deciding the gate.The targeted runtime repro passes, but the required adjacent jump-affordance snippet fails on its existing `textContent` system-tail assertion even though the visible `innerText` is clean and the system-line-specific snippet passes. I? collecting final diffs/status and will record that validation result explicitly in the implementation artifact.I have enough validation evidence now: the scoped Enter/undo/redo runtime passes in a cold fixed-port runtime, full tests/build pass, and one required adjacent snippet fails on a textContent-based hidden-token assertion. I? writing the implementation handoff with that distinction instead of hiding the failed check.I? writing `docs/agent/runs/BLP-3/implementation.md` now with the final patch summary, exact validation commands, the cold-runtime CDP evidence, and the adjacent snippet failure called out for code review.The implementation handoff is written. I? doing a final status/diff sanity check and a whitespace check before closing.One cleanup step remains: I? closing the disposable Obsidian debug runtime now that the fixed-port validation evidence is captured.Implemented BLP-3 and wrote the handoff at [implementation.md](C:\Users\stati\Documents\Codex\2026-06-13\hermes\outputs\plane-symphony-like-demo\workspaces\BLP-3\docs\agent\runs\BLP-3\implementation.md:1).

Changed [selectiveEditor.ts](C:\Users\stati\Documents\Codex\2026-06-13\hermes\outputs\plane-symphony-like-demo\workspaces\BLP-3\src\shared\utils\codemirror\selectiveEditor.ts:74) so line-count range sync is owned by the range state fields, while `preventModifyTargetRanges` only blocks out-of-range edits. Added the Enter/undo/redo regression in [selectiveEditor.systemLine.test.ts](C:\Users\stati\Documents\Codex\2026-06-13\hermes\outputs\plane-symphony-like-demo\workspaces\BLP-3\src\shared\utils\codemirror\__tests__\selectiveEditor.systemLine.test.ts:133).

Validation:
- `CI=1; corepack pnpm install --frozen-lockfile` passed.
- Targeted Jest passed: 1 suite, 4 tests.
- Full Jest passed: 38 suites, 214 tests, with the existing worker-teardown warning.
- `corepack pnpm run build-with-types` passed.
- Cold fixed-port Obsidian/CDP repro passed: `Enter` `[5,9] -> [5,10]`, `Ctrl+Z` back to `[5,9]` with no leaked paragraph, redo back to `[5,10]`.
- `debug-inline-edit-system-line.js` passed.
- `inline-edit-embed-jump-affordance.js` still fails on a `textContent` hidden system-tail assertion; I documented it as an adjacent validation risk for code review.

I also closed the disposable Obsidian runtime after capturing evidence.

I’ll first load the repo-owned task contract and the relevant prior memory, then I’ll inspect the existing investigation/design artifacts before touching code. I’m using the Obsidian runtime-debug skill because this bug concerns real embed scrolling/editor behavior.The memory pass points to the repo’s isolated Obsidian/CDP launcher and prior BLP issue triage: reload the app before trusting runtime style/DOM checks, keep probes state-safe, and prefer repo scripts over skill-bundled fallbacks. The named runtime skill path is repo-local here, so I’m reading that copy now along with the stage contract.The source snapshot has the full bug claim and marks the item `cdp-required`, so this is not context-blocked. I’m reading the implementation stage and prior stage artifacts next; those define what is already accepted and what patch boundaries I’m allowed to implement.The fix-design review is accepted and gives a tight patch: skip only passive Live Preview `setCursor`/`scrollIntoView`, preserve outliner behavior, and do not add a host scroll guard unless post-patch runtime evidence forces a design revisit. I’m loading the remaining required validation docs, then I’ll inspect the existing inline-edit tests so the regression coverage follows local patterns.The worktree is clean except for the existing run artifact directory. I’ve confirmed the current source has the accepted split: outliner calls the shared mount path with `origin: "outliner"`, while Live Preview uses `origin: "live-preview"`, so the patch can be narrowly keyed off those options.The existing tests already exercise private helpers via `(engine as any)`, and the mount code is injectable enough to mock leaf creation and parser output. I’m going to add focused unit coverage around `mountInlineEmbedCore` rather than widening the test surface.The mount path already dispatches range annotations before the cursor/reveal block, so the code change can skip only the problematic side effect and leave the selective-editor state unchanged. I’m checking the test environment details now so the mock mount test does not depend on real Obsidian internals.Before editing, the concrete change is: add a tiny passive-mount classifier in `InlineEditEngine.ts`, compute it after the existing early exits, and wrap only the embedded editor cursor/reveal block. I’ll also add a new focused test file that mocks the leaf and CM view so it can assert passive Live Preview skips `setCursor`/`scrollIntoView`, outliner still calls them, and range annotation dispatch still happens.I’m applying the source patch and adding the regression tests now. The tests will intentionally not simulate a host scroll guard, because the accepted design rejected that as first-patch behavior.The source patch is in place: passive Live Preview mounts are classified and now skip only the cursor/reveal block. Next I’m adding the focused tests around that split and click/focus readiness.The unit coverage is added. I’m running the inline-edit engine test subset first; if that exposes fixture issues, I’ll tighten the mocks before moving to broader validation.The targeted inline-edit suite passes, including the new passive-mount cases. Jest reported a worker teardown warning, so I’ll keep an eye on whether it appears in the full suite, but the subset itself is green.Dependency install is clean and the lockfile was already current. The diff is still the narrow accepted gate; I’m running the required broader test suite and type/build next.The full Jest suite passes: 40 suites and 219 tests. The same Jest worker teardown warning appears after completion, so I’ll report it as a non-failing warning rather than hiding it.The type check and production bundle build pass, updating the runtime bundle. I’m moving to the required fixed-port Obsidian/CDP validation now and will reload/start the isolated runtime before trusting the post-build behavior.Port `19225` is not currently serving a CDP target, so I’m launching the repo’s disposable Obsidian debug runtime on that fixed port.The isolated runtime is ready on `19225`, with BLP loaded from this worktree as version `2.0.15`. I’ll run the scroll stability probe and existing inline-edit CDP regressions against that runtime.CDP target selection is clean. I’m inspecting the existing temporary probe interface briefly so I can interpret its output correctly instead of treating a diagnostic JSON dump as a pass/fail by name alone.The existing probes are diagnostic and restore settings in `finally`. I’m running the passive-mount instrumentation first; the key post-fix signal is zero plugin-origin `scrollIntoView` events from `mountInlineEmbedCore`, plus a small no-input scroll delta.The passive-mount probe now shows zero `editor.scrollIntoView` and zero `cm.dispatch.scrollIntoView` events, and the visible marker stays at `tail 24` through mount. Numeric `scrollTop` shifts by `-105.5` while scroll height changes, which is much smaller than the pre-fix `-1205.5`/`+3877.5` evidence and does not move the visible content marker. Next is the scroll-while-enabled probe.The scroll-while-enabled probe also shows zero scroll-reveal events after the patch. Settled deltas are now `-301`, `-13.5`, `-82.5`, and `+2`, compared with the pre-fix large mount-correlated offsets; I’m adding one temporary runtime probe for the remaining required click/focus check.The click/focus probe is in `.tmp/BLP-4` and restores settings/file state. I’m running it against the same fixed runtime.The click/focus runtime probe passes: the embedded editor receives focus, visible text remains stable, and host scroll delta is `0`. I’m running the existing inline-edit CDP regressions now, including the native jump affordance and padding checks.The mandatory jump-affordance snippet failed on its hidden-tail assertion, while its structural checks had already reached one active inline embed, one direct native link, one host, and one root. I’m checking whether this is a visible UI regression or the known `textContent` false-positive pattern before deciding whether to touch the validation snippet.The follow-up eval confirms this is the known hidden-DOM false positive: `embed.innerText` and the inline root’s `innerText` contain only the target block, while `textContent` includes hidden native preview/source metadata. I’m making a minimal validation-snippet repair to use visible text, then I’ll rerun the regression.The snippet repair is limited to `innerText` fallback for the existing visible-tail assertion. I’m rerunning the jump-affordance regression now.The repaired jump-affordance regression passes: the native direct link stays visible/clickable, navigation works before and after inline edit, and remount keeps one host/root. I’m running the bottom-padding regression next.The bottom-padding regression passes with `paddingBottom: 0px` and the native jump link present. I’m also running the leaf-switch remount regression because the touched path is the shared inline embed mount path.The leaf-switch remount regression passes as well. I’m checking the final diff/status before writing the implementation artifact, then I’ll run the workflow checker after the artifact is in place.The current tracked diff is the source gate plus the validation-snippet `innerText` fix; the new unit test is still untracked, and `main.js` did not change after the production build. I’m verifying the built bundle contains the passive-mount gate so runtime and source are aligned.`main.js` is not tracked by Git in this worktree, but its timestamp updated during the build. I’m checking the built file directly with a simpler literal search because the previous regex search expanded outside the intended path.I’m writing the implementation artifact with the exact patch scope and validation results, including the one non-product CDP snippet repair and the remaining Android/mobile validation risk. After that I’ll run the repo workflow check and clean up the disposable Obsidian process.The implementation artifact is written. I’m running the workflow checker now; if it passes, I’ll stop the disposable Obsidian process and capture final status.The workflow check passed. I’m stopping the isolated Obsidian process launched for this run and then I’ll record final status.The disposable Obsidian runtime has been stopped. I’m doing the last status check and then I’ll patch the artifact if the final state needs to be recorded more exactly.The final status has the expected source/test/artifact changes, and the Obsidian process is gone. I’m adding the workflow-check result and final status to the artifact for completeness.I updated the artifact with the final status and cleanup result. I’m rerunning the workflow checker once more after that artifact edit.The workflow checker still passes after the artifact update. I’m doing one last status/citation check, then I’ll hand off the concise completion summary.Implemented BLP-4 and wrote the required artifact at [implementation.md](C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-4/docs/agent/runs/BLP-4/implementation.md).

Changed:
- `InlineEditEngine.mountInlineEmbedCore` now skips passive Live Preview `setCursor` / `scrollIntoView(..., true)`.
- Added focused mount-scroll tests.
- Repaired the jump-affordance CDP snippet to use visible text via `innerText`, after confirming `textContent` was reading hidden native preview DOM.

Validation passed:
- `corepack pnpm test -- src/features/inline-edit-engine`
- `corepack pnpm test`
- `corepack pnpm run build-with-types`
- fixed-port Obsidian/CDP runtime on `19225`
- BLP-4 passive-mount, scroll-while-enabled, and click/focus probes
- jump-affordance, bottom-padding, and leaf-switch CDP regressions
- `corepack pnpm run agent:workflow-check`

Remaining caveat: Android/mobile Obsidian was not directly validated; the implementation is validated in the repo’s Windows Desktop Obsidian/CDP runtime. Jest also still prints a non-failing worker teardown warning after passing.


I’ll use the Obsidian runtime-debug skill because this is a runtime investigation involving Obsidian editor/plugin lifecycle behavior. I’m also doing a quick memory lookup for prior BLP runtime conventions before reading the required stage inputs.The memory pass found prior BLP runtime tooling notes: use the repo’s isolated Obsidian launcher, prefer `npm.cmd`/Corepack-safe commands on Windows, and keep diagnostic probes state-restoring. I’m now reading the required run contract and issue inputs before touching runtime.The source issue has the full bug claim, so this is not Context Blocked. The stage is investigation-only: I can use CDP/runtime probes and write the canonical investigation artifact, but I should not edit product source, tests, package metadata, snippets, or specs.I’ve got the stage contract and the required CDP rule now. Next I’m checking the fixed-port runtime prerequisites exactly as `docs/agent/cdp-validation.md` specifies, because this task is labeled `cdp-required`.Dependencies are already installed from the repo lockfile. I’m moving through the rest of the runtime gate: local module availability, typed build, then checking whether port `19225` is already the BLP debug target before launching anything.The typed build passed and produced the plugin bundle for the disposable runtime. Port `19225` was not reachable, so I’m launching the fixed-port isolated Obsidian environment now.The `pnpm run ... -- -Port 19225` wrapper is failing because it passes a literal `--` through to PowerShell and the script treats it as an ambiguous parameter. I’m retrying the same repo-owned launcher directly with `-Port 19225`, still on the fixed port required by the stage.The direct fixed-port launcher also failed: it timed out while trying to set the profile-local trust key because CDP never became reachable on `127.0.0.1:19225`. I’m doing one final reachability/process check so the blocked handoff has precise evidence instead of only the launcher stack.The final check confirms there is no Obsidian process and CDP on `19225` still refuses connections. Per the investigation stage and CDP validation rules, I’m stopping at a Runtime Blocked artifact instead of making a static root-cause claim.I’m writing the handoff now. It will explicitly separate prior accepted RCA text from what this run verified: dependencies/build passed, fixed CDP runtime failed, so no current RCA/fix plan is established in this stage.
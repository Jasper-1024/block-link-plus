I’ll use the repo’s agent workflow plus the Obsidian runtime/debug context, because this stage is explicitly about a BLP issue and has a repo-owned fix-design contract. I’m starting by reading the required stage inputs and doing a light memory lookup for prior BLP-specific guidance.The prior memory points to BLP issue-triage and inline-edit work, so I’ll keep that in mind but verify against this worktree’s current artifacts. Next I’m reading the stage contract and required context files before touching the output artifact.The fix-design stage is documentation-only: no source/test/package/spec edits, and it requires an accepted RCA before design. I’m now checking the canonical RCA inputs, validation format, and current worktree state so the design can cite concrete files and stay within the accepted child-issue boundary.The RCA review is present for `BLP-2` and accepted, so this is not context-blocked. I’m checking the exact lifecycle methods and existing test surfaces now, then I’ll write only the required `fix-design.md` artifact.The existing evidence proves the remount path: non-forced refresh misses the returned embed, forced rescan restores it. I’m reading the relevant code chunks now to choose a narrow implementation shape rather than recommending a broad refresh everywhere.OpenSpec confirms this is a direct bug restoration unless the proposed fix changes intended behavior. I’m doing the active-change/spec conflict check now so the handoff can explicitly state whether implementation needs an OpenSpec proposal.The current lifecycle already has idempotent processing guards and shell cleanup paths; the gap is when an unchanged observed MarkdownView is not repopulated with existing embed DOM. I’m checking those guards before committing the design wording, since the reviewer should be able to attack duplicate-mount and observer-churn risks directly.I have enough code context now. The design I’ll write is a small `InlineEditEngine` change: keep cleanup, but after layout-driven hidden cleanup, explicitly rescan only visible Live Preview Markdown views that still contain unloaded native embed shells rather than depending on a broad transaction or parser path.The Jest setup has an Obsidian mock, so the implementation can add a focused unit test for observer rescan behavior in addition to a CDP regression. I’m checking the mock shape to keep that test realistic for the later implementation stage.I’m going to create the fix-design artifact now. It will recommend a specific implementation path, list alternatives that were rejected, and define the unit/CDP validation the implementation worker must run.The artifact is written. I’m doing a quick verification pass for required sections and final worktree state, then I’ll close with the exact file path and any validation caveats.Created the required fix-design artifact: [fix-design.md](C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-2/docs/agent/runs/BLP-2/fix-design.md).

Verdict is `ready-for-review`. The design recommends a narrow `InlineEditEngine` visible-return rescan: track hidden-to-shown transitions for already-observed Live Preview `MarkdownView`s and queue existing embed DOM for processing on return, without broadening into Reading View, undo, or other GitHub #33 sub-bugs.

No source/tests/package/OpenSpec files were changed. I did not run product tests because this stage is documentation-only; final `git status --short` remains `?? docs/agent/runs/BLP-2/`.


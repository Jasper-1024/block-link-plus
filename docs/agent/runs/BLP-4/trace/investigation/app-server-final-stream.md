I’ll use the Obsidian runtime debugging guidance plus the BLP-specific debugging notes here, because this task is explicitly a runtime investigation for a Block Link Plus Obsidian behavior. I’m first loading the repo contract, stage spec, source issue, and relevant local memory before making any claims.The source snapshot is present and includes the full user claim, so this is not context-blocked. I also found prior memory saying issue #36 was known as a more architectural Android/embed scroll problem, but I’m treating that only as orientation until current runtime evidence is collected.The stage spec confirms this is middle-flow only: no product code, tests, generated files, CDP snippets, or OpenSpec changes. I’m going to run the fixed-port CDP validation path next, and only use static source reading to pick a minimal repro and identify candidate owners.There is already an `investigation.md` from a prior attempt, marked Runtime Blocked after a timed-out custom probe. Because there is no RCA review artifact, I’m continuing the same investigation artifact and rerunning the repo’s fixed validation flow rather than relying on that stale blocker.Dependencies are already installed from the lockfile. I’m doing the required build/type check now so the debug vault loads the current plugin bundle rather than an old artifact.The build/type check passed and produced the plugin CSS bundle as part of the standard validation path. I’m checking the fixed CDP port before launching anything, per the stage rule.Port `19225` is not currently reachable, so I’m starting the repo-owned isolated Obsidian runtime on that fixed port now. If this cannot verify `blockLinkPlusLoaded`, the artifact will stay Runtime Blocked.The isolated runtime started cleanly on `19225` with Obsidian `1.12.4` and Block Link Plus `2.0.15` loaded from this worktree. Next I’m going to reproduce the scroll behavior with a disposable note containing many embeds and instrument scrollTop changes at runtime.There are several plausible owner areas from the source map: native multiline embed rendering, InlineEditEngine’s Live Preview takeover, and file-outliner scroll preservation. I’m checking the active OpenSpec/spec landscape and the user’s attached video before narrowing the repro.The GitHub attachment downloaded successfully as an MP4 under `.tmp/BLP-4`. I’m going to extract lightweight metadata and a few frames so the investigation can cite what the reporter actually showed without depending only on the title.The machine does not have `ffmpeg`/`ffprobe` on PATH, so frame extraction is not available through the usual tools. I’ll keep the downloaded attachment as source evidence and focus the runtime reproduction inside the isolated Obsidian instance.I’m correcting the PowerShell syntax for a quick module check. If a local Python video library is already present, I can still extract frames without installing anything.The relevant shipped defaults make block and heading embeds inline-editable, while whole-file embeds are off by default. I’m going to probe block embeds specifically, with inline edit on and then off as a control, because that matches the default plugin surface most likely to mutate embedded note DOM during scrolling.There are existing temp probes under `.tmp/BLP-4`, including one that directly forces inline-edit remounting while preserving the host scroll position. I’m running the smaller one first so we can get evidence quickly before attempting the heavier scrolling probe.The smaller probe did not reproduce a jump, but it also scrolled past the embedded blocks so there were no active embeds left at the measured position. I’m running the broader scroll probe now, with a longer timeout than the earlier failed attempt.The broad probe also did not snap back to the top on Windows Desktop, but it showed inline edit changes the scroll/height dynamics while embeds virtualize. I’m adding one more temp probe to instrument the live mount path itself: whether enabling inline edit while already scrolled calls embedded editor `scrollIntoView`/CodeMirror measure paths and whether that moves the host scroller.The instrumented probe is in `.tmp/BLP-4`, so it stays outside product code and reusable CDP snippets. I’m running it against the live runtime now.The instrumented mount probe reproduced a large host-scroll relocation in the desktop runtime: while already scrolled, enabling inline edit moved the host from `3273.5` to `7151` and changed the visible block from `tail 24` to block `46`. I’m rerunning a trimmed version of the same probe to capture the scroll-related call stacks cleanly for the handoff.The temp probe now returns filtered `scrollIntoView` and plugin-origin `requestMeasure` stacks. Running it once more should give enough evidence to bound the RCA.The trimmed run confirmed the scroll relocation is repeatable but direction depends on timing: one run moved down by `+3877.5`, the next moved up by `-1205.5`. The common fact is that the visible viewport changes without user scrolling while inline-edit embeds are being mounted, and the trace points into `InlineEditEngine.mountInlineEmbedCore`.I have enough evidence for the investigation handoff. I’m going to replace the stale Runtime Blocked artifact with the current runtime findings, including the remaining Android-specific caveat and the bounded root-cause owner.The investigation artifact has been updated with the current runtime evidence and RCA boundary. I’m running the workflow checker and then recording the final git status.The repo workflow check passed, and the final git status still only shows the BLP-4 run directory as untracked. I’m adding that final status to the artifact so the handoff is self-contained.I made a documentation-only artifact update, so I’m rerunning the workflow checker once after that small edit.Updated the investigation handoff at [investigation.md](</C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-4/docs/agent/runs/BLP-4/investigation.md:1>).

Key result: runtime evidence confirms a BLP-owned scroll relocation bug in Live Preview embeds. The instrumented CDP probe showed `InlineEditEngine.mountInlineEmbedCore` moving the host editor scroll without user input while passive inline-edit embeds mount, with captured `editor.scrollIntoView(..., true)`, `cm.dispatch(scrollIntoView)`, and host `requestMeasure()` activity from that path. The exact Android 13 “back to initial position” pattern remains unverified because the repo runtime is Windows Desktop Obsidian (`app.isMobile === false`).

Validation done:
- `corepack pnpm install --frozen-lockfile`
- `corepack pnpm run build-with-types`
- isolated Obsidian/CDP runtime on port `19225`
- disposable CDP probes under `.tmp/BLP-4`
- `corepack pnpm run agent:workflow-check` passed

No product source/test/generated/OpenSpec files were edited. Final `git status --short` remains `?? docs/agent/runs/BLP-4/`.


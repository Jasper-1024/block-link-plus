I’ll follow the repo-owned workflow for the BLP-4 fix-design stage, and I’m using the Obsidian runtime-debug skill because this is an Obsidian editor/scroll behavior bug. First I’m reading the required stage context and the relevant skill guidance before making any design claims.The issue snapshot contains the full GitHub bug claim and the `cdp-required` label, so this is not context-blocked. I also hit a path mismatch for the runtime-debug skill and am reading the repo-local copy now along with the fix-design stage contract and prior artifacts named for this stage.The accepted RCA is present, so this stage can design rather than block. The boundary is narrow: passive inline-edit embed mounting moves the host Live Preview scroll; Android-specific direction/frequency remains validation risk rather than the core owner-layer proof.There is already a prior fix-design artifact and a design review marked `needs-revision`. I’m treating this as a revision pass: keep the accepted owner layer, address the reviewer’s guard/focus objections, and only update the fix-design artifact.The current source confirms the passive/live-preview and outliner paths share `mountInlineEmbedCore`, with the passive path passing `origin: "live-preview"` and the outliner path passing `origin: "outliner"`. I’m checking the exact mount sequence now so the revised design can specify guard timing without leaving implementation choices implicit.The revision I’m making narrows the first implementation: remove the passive selection/reveal side effect and do not ship a host scroll guard in the first patch. That avoids making an Android touch-scroll guard decision before the direct `scrollIntoView` cause has been removed and measured.The fix-design artifact is revised. I’m running the repo workflow check now, then I’ll stamp the artifact with the actual validation and final git status.The workflow check passed. I’m collecting the final git status and then I’ll update the artifact status lines so the handoff is self-contained.Git still shows only the BLP-4 run directory as untracked, matching the repo workflow’s expected artifact state. I’m applying the final status-line update now.The artifact now records the passed workflow check. I’m doing one final workflow-check/status pass after that last metadata edit.The final artifact is in place and the workflow check still passes. I’m just verifying the memory citation details for the final note because I used the repo memory registry during the initial context pass.Updated [fix-design.md](C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-4/docs/agent/runs/BLP-4/fix-design.md) for the BLP-4 fix-design revision.

Key change: the design now recommends the minimal first implementation: gate passive Live Preview `setCursor` / `scrollIntoView(..., true)` in `InlineEditEngine.mountInlineEmbedCore`, with no host scroll guard in the first patch. It also makes click/focus validation explicit and keeps any guard as a post-CDP fallback only.

Validation: `corepack pnpm run agent:workflow-check` passed. No product source, tests, generated files, CDP snippets, package metadata, or OpenSpec specs were edited. Final git status is still `?? docs/agent/runs/BLP-4/`.


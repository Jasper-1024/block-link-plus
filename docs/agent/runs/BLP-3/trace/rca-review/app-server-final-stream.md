I? treating this as an `rca-review` handoff, not a fix. I? starting with the repo contract and stage docs, then I?l verify whether prior BLP context in memory is relevant before I assess the issue evidence.I? pulling the stage contract, the issue snapshot, and the two debugging skills in parallel. If the issue needs runtime proof, I?l follow the repo? CDP-first rule before I judge the existing RCA.I found a prior BLP note that explicitly treats issue `#33` as a cluster, including `Enter` and `Ctrl+Z` overflow. I? using that as context only; the current judgment still has to come from this worktree? stage contract and the issue snapshot.The child issue is explicitly scoped as a follow-up to the parent cluster RCA, so I? reading both the current `BLP-3` investigation artifact and the parent `BLP-1` RCA. After that I?l decide whether runtime reproduction is still missing or whether I need to prove a narrower mechanism.`BLP-1`? RCA artifact isn? present in this worktree, so I? treating that as missing local context rather than inventing from the Plane text. I have the current `BLP-3` investigation artifact; next I? validating its static code claims against the source, then I?l decide what runtime proof I still need to rerun myself.I have enough context to judge the current investigation rigorously.

Plan:
1. Re-run the mandatory CDP preflight in this worktree so the review isn? based on prior logs.
2. Reproduce the exact `Enter -> Ctrl+Z` leak in the disposable Obsidian runtime and inspect the live range state.
3. Cross-check the claimed owner path in `selectiveEditor.ts` against the observed transaction behavior.
4. Write `docs/agent/runs/BLP-3/rca-review.md` with a strict verdict and only the remaining evidence gaps, if any.The install command didn? fail, but it also returned unusually fast after a reinstall prompt, so I? verifying the actual dependency state before I trust it. After that I?l build and launch the isolated runtime.I? at the point where static reading won? move the verdict. I? switching to a live probe: create a minimal source/host note pair, mount the inline embed, then capture the CM6 visible/editable ranges before `Enter`, after `Enter`, and after a real undo path.I found the prior runtime probe script in the runner logs, which is useful because it shows the exact interaction shape that already reproduced the bug. I? re-running that mechanism now against a fresh disposable vault and current CDP port so the review verdict is based on live output, not on copied conclusions.The live repro is confirmed. I? doing one last adversarial pass on interpretation: checking whether the `transactionFilter` actually emitted the expected range-update spec on undo, or whether the current investigation is still skipping over a CodeMirror behavior detail.I?e already proved the branch conditions now: on undo, `numberNewLines=-1`, `touchesInside=true`, and the expected end line is `9`, but the live state still ends at `10`. I? checking one deeper layer: whether the merged CM transaction even carries the shrink annotations, because that decides whether the remaining gap is ?ilter never emitted them??or ?ield update ignored them.?he last probe tightened the RCA materially: the `Enter` transaction does carry two range-array annotations `[5,10]`, but the `undo` transaction carries none at all. That means the current investigation? remaining uncertainty is narrower now: the shrink annotations are absent by the time the merged undo transaction reaches dispatch, despite the undo change satisfying the same inside-range branch conditions.I have the adversarial conclusion now. The missing piece wasn? ?hich branch should shrink on undo??anymore; it was ?hy that shrink branch never fires.??The answer is local and concrete: CodeMirror history dispatches undo with `filter: false`, and this plugin? range-maintenance logic lives in a regular `transactionFilter`, so the undo transaction bypasses the shrink annotations entirely.I? writing the RCA review artifact now. It? a docs-only edit: one new file under `docs/agent/runs/BLP-3/` with the verdict, the adversarial findings, and the now-proven undo/filter mechanism.[rca-review.md](C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-3/docs/agent/runs/BLP-3/rca-review.md) is written with verdict `accepted`. I reran the CDP gate on this worktree, reproduced the `Enter -> Ctrl+Z` leak, and tightened the RCA beyond the current investigation: `preventModifyTargetRanges` lives in a regular `EditorState.transactionFilter`, while CodeMirror history builds undo with `filter: false`, so the undo transaction bypasses that range-maintenance filter entirely. The result is that the shrink annotations are absent on undo, the range stays `[5,10]`, and the next paragraph leaks into the inline-edit embed.

I did not change product code or tests. Validation run: `corepack pnpm install --frozen-lockfile`, `node -e "require.resolve('ws'); console.log('ws-ok')"`, `corepack pnpm run build-with-types`, `corepack pnpm run obsidian:debug-env`, and live CDP repro probes on port `19227`. I did not run `corepack pnpm test`. The worktree already had runner-generated untracked files; my only intentional repo edit was the new RCA review artifact.

I? treating this as a stage-bound runtime investigation, not a speculative fix. First I? reading the repo-owned contract, the issue snapshot, and the stage instructions, then I?l run the required Obsidian/CDP preflight before making any root-cause claims.I found prior project memory that this is already treated as an `#33` sub-bug after a separate padding fix, and the current gap is still the undo-decoration mechanism. I? loading the RCA review, evidence format, and CDP validation docs next, then I?l verify the disposable Obsidian runtime instead of inferring from source.The repo stage docs are clear: this run must stop at evidence and RCA handoff, and there is no existing local `BLP-3` RCA review to answer in-place. I? locating the earlier `BLP-1` artifacts and the current CDP scripts now so I can reproduce the exact Enter-then-undo path instead of doing broad source reading.Using `obsidian-runtime-debug` first, then `blp-enhanced-list-ui-debug` for the inline-edit/UI ownership map. Plan:
1. Run the mandated CDP preflight and confirm the disposable Obsidian target is healthy.
2. Reproduce the Enter then undo overflow in a clean note and capture runtime facts with the existing inline-edit snippets.
3. Instrument the selective-editor / decoration path just enough to prove which update branch fails after undo.
4. Write the bounded investigation handoff in `docs/agent/runs/BLP-3/investigation.md`, including exact commands, confirmed evidence, and remaining risks.I? at the mandatory runtime gate now. I?l run the repo? preflight exactly as documented and stop at a Runtime Blocked handoff if the disposable Obsidian environment does not come up cleanly.The disposable Obsidian runtime is up and the plugin is loaded, so the run is no longer blocked on CDP. Next I? searching for existing repro snippets and prior `#33` investigation notes, then I?l drive a minimal note through the Enter + undo sequence and capture the broken DOM state.I found prior repo-local triage notes for this exact Enter/Ctrl+Z symptom under `.tmp/issue-triage-2026-06-12.md`. I? using that only as a repro lead, not as accepted evidence; the next step is to compare it against the current runtime and the current selective-editor code path.I?e switched from broad file reading to live instrumentation. Rather than create repo code changes, I? using CDP eval scripts against the disposable vault so I can keep the investigation in middle-flow and still get transaction-level evidence.The runtime repro is confirmed on the current worktree with a clean temporary note: Enter adds the child line, Ctrl+Z restores the source text, and the inline embed still leaks the next paragraph. I? now distinguishing ?ange state wrong??from ?ange state right but view decorations stale??by probing the live CM state and forcing remeasure/repaint paths.I have the core mechanism narrowed now: the line-range state expands from `[5,9]` to `[5,10]` on `Enter`, which is plausible, but the `undo` transaction leaves it at `[5,10]` instead of shrinking back. I? running one final pass with branch-condition logging so the handoff can point to the exact `preventModifyTargetRanges` decision path instead of only saying ?anges stayed stale.? have enough evidence to write the handoff. Before I touch the repo, I? collecting exact line references for the CM range-update code and then I?l add only the canonical investigation artifact under `docs/agent/runs/BLP-3/`.I? writing the canonical handoff now. This is the only repo edit in the run: a new investigation artifact with the runtime proof, source references, and bounded next step.Created [investigation.md](C:\Users\stati\Documents\Codex\2026-06-13\hermes\outputs\plane-symphony-like-demo\workspaces\BLP-3\docs\agent\runs\BLP-3\investigation.md:1).

The runtime gap is now closed. In a fresh disposable Obsidian vault, the inline-edit range starts at `[5,9]`, `Enter` correctly grows it to `[5,10]`, and `Ctrl+Z` restores the source text but leaves the tracked range at `[5,10]`, which exposes `Paragraph block for regular embed ^para1`. The bounded owner is `preventModifyTargetRanges` in `selectiveEditor.ts`: the undo transaction still satisfies the inside-range line-count branch (`touchesInside=true`, `numberNewLines=-1`, expected end `9`), but the actual end line stays `10`.

No product code or tests were changed. I ran install/build/CDP preflight plus targeted runtime probes; full `corepack pnpm test` was not run.

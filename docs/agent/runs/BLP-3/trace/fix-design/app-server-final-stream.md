I’ll use the Obsidian runtime/debug and BLP list UI skills for this issue, then follow the repo’s stage contract before writing the fix-design artifact. First I’m gathering the required source and workflow documents so the handoff matches the runner’s expectations.The source snapshot is complete and confirms this child scope is only the Enter+undo visible range overflow. I’m reading the stage spec and existing run artifacts next; the key risk is proving the exact decoration/mapping branch before proposing an implementation.The stage contract says this is design only: no source, test, generated, CDP snippet, or spec edits. I also opened OpenSpec because this is a design/planning stage; for a scoped bug fix restoring intended behavior, it should only be a gate note unless the design uncovers a behavior change.RCA review is accepted, and it sharpened the mechanism: undo is a CodeMirror history transaction with `filter: false`, so the existing `transactionFilter` cannot emit the shrink annotations. I’m now checking the exact extension shape and CodeMirror APIs so the design points at an implementable hook, not just the owner file.The implementation hook is clear now: only the “cancel/allow edits” part belongs in a transaction filter. The line-range synchronization itself only adds metadata, so it can move to `transactionExtender`, which CodeMirror explicitly runs even when `filter: false`.I’m checking OpenSpec status and relevant capability specs before writing the design. This should let the artifact explicitly say whether implementation needs a proposal or can proceed as a contained bug fix.The OpenSpec check found an existing `inline-editing-embeds` capability and no active change for this bug. The current spec already requires block inline-edit rendering; this design can stay in the bug-fix path and does not need a new OpenSpec proposal unless review decides range persistence semantics need to be made normative.I’m doing the fixed-port CDP preflight now because this item is marked `cdp-required`. If the runtime is unavailable on the required port, the design will rely on the accepted RCA’s evidence but I’ll record the current preflight result instead of pretending a fresh runtime check passed.Fixed port `19225` is reachable and points at a BLP Obsidian target. I’m checking the active vault/plugin state on that target, then I’ll write the design artifact.One detail worth preserving in the design: inline-edit mount already dispatches range initialization with `filter:false`, and the range fields already prefer `setContentRange` / `setEditableRange` effects before annotations. That makes an effects-returning extender a narrow change; it should not require touching `InlineEditEngine`.The test design will need to include CodeMirror history explicitly; the current unit tests only cover decoration output and line-range growth. I’m keeping the proposed regression focused on the state fields because the accepted RCA says the DOM leak is downstream of stale visible/editable ranges.I have enough context to write the fix design. The artifact will recommend a two-part split: keep the filter for edit rejection, move line-range synchronization into a `transactionExtender`, and add a history-backed regression that proves `Enter -> undo` restores the range.I’m editing only the required stage artifact now: `docs/agent/runs/BLP-3/fix-design.md`. No product code, tests, generated files, CDP snippets, or OpenSpec files are being changed in this fix-design stage.The fix-design artifact is written. I’m doing a quick self-check for the required sections, current git status, and any accidental source/test/spec edits before closing.The artifact has the required sections, and the link check found no local `file://` or drive-letter markdown links. Git still shows the runner-provided files and `docs/agent/runs/` as untracked; I only added the fix-design artifact under that run directory.Created the fix-design artifact at [fix-design.md](C:/Users/stati/Documents/Codex/2026-06-13/hermes/outputs/plane-symphony-like-demo/workspaces/BLP-3/docs/agent/runs/BLP-3/fix-design.md).

Verdict is `ready-for-review`. The recommended fix is to keep `preventModifyTargetRanges` as the edit-blocking `transactionFilter`, move line-range synchronization into a new CodeMirror `transactionExtender`, and add a history-backed Jest regression for `Enter -> undo` restoring `[5, 9]` without leaking the following paragraph. The design also calls out redo validation because it shares the same `filter: false` history path.

Validation performed for this design stage:
- Required sections verified in the artifact.
- No `file://` or drive-letter markdown links found in the artifact.
- Fixed-port CDP preflight on `19225` found the BLP Obsidian target, vault `blp`, and `block-link-plus` loaded.
- No product source, tests, generated files, package metadata, or OpenSpec files were edited.

I did not run Jest/build because this was the design-only stage and no implementation files changed.
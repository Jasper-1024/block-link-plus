# 2.0.20 release validation

- Scope: approved BLP-12 embedded Find implementation and BLP-18 completion fix, already merged into master.
- Updated package/manifest/version compatibility map, three-language changelogs, Inline Edit usage guides, and in-app release notes. Dependencies are unchanged; no lockfile update is needed.
- Reproduced stale release content on isolated Obsidian 1.13.6 (CDP 19234): simulating 2.0.18 -> 2.0.19 opened the modal but displayed 2.0.0 notes. Documentation also stopped at 2.0.17. The user's historical missing-popup symptom was not reproduced; do not claim its cause is established.
- Regression: `corepack pnpm exec jest --runInBand release-notes` failed on the old fallback and passed after adding current-release content.
- `corepack pnpm exec jest --runInBand`: 51 suites, 303 tests passed.
- `corepack pnpm run build-with-types`, `corepack pnpm run agent:workflow-check`, and `git diff --check` passed.
- Fresh isolated runtime: CDP 19235, blp-release-2020, Obsidian 1.13.6, built plugin 2.0.20. Persisted lastSeenVersion=2.0.19 then disabled/enabled the plugin: current release notes appeared. Closed using the modal's Close button, disabled/enabled again: no modal, persisted lastSeenVersion=2.0.20.
- Probe correction: Obsidian 1.13.6 has no `.modal-close-button`; an initial test left old modals open and incorrectly reported repetition. Retest explicitly asserted no existing modal before upgrade and used the actual Close button.
- Publication: push master and the new 2.0.20 tag; existing GitHub workflows build release assets and deploy the documentation. Never overwrite old tags.

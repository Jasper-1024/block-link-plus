# Tasks

- [x] Implement in-memory block index for Enhanced List system-line blocks.
- [x] Add a block picker modal (search + render + insert).
- [x] Add commands for inserting block references and embeds.
- [x] Add `((` trigger in Live Preview (replaces typed `((` with chosen reference).
- [x] Add unit tests for the index/parser.
- [x] Validate behavior in Obsidian via 9222/CDP (modal opens; insertion works).


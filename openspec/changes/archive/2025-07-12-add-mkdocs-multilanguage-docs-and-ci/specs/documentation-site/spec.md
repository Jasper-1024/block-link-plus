## ADDED Requirements
### Requirement: Provide multi-language documentation structure
项目 SHOULD 提供中/英/繁体中文三套文档内容，并保持页面对应关系与导航一致性。

#### Scenario: Language-specific docs folders
- **WHEN** 维护者查看仓库文档目录
- **THEN** 存在 `docs/`（默认语言）、`docs/en/`、`docs/zh-TW/`，且核心页面在各语言下均有对应文件

